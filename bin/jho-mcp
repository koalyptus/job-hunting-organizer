#!/usr/bin/env node
import { startServer } from '../dist/mcp/server.js';

startServer().catch((err) => {
  process.stderr.write(`jho-mcp: ${err instanceof Error ? err.message : String(err)}\n`);
  process.exit(1);
});
